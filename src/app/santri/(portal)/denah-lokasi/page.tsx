import { DenahLokasiClient } from "@/components/santri/denah-lokasi-client";

export const metadata = {
  title: "Santri - Denah Lokasi Sakan",
};

export default function SantriDenahLokasiPage() {
  return <DenahLokasiClient />;
}
